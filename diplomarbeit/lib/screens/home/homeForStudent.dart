import 'package:flutter/cupertino.dart';

class HomeForStudent extends StatelessWidget {
  const HomeForStudent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Hallo"),

    );
  }
}
